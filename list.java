public class list{
    static class Node{
        int data;
        int next;
        Node(int data){
            this.data=data;
            this.next=null;
        }
    }
    static Node head;
    static Node tail;
    public static Node insertBegin(int data){
        Node newNode=new Node(data);
        if(head==null){
            return newNode;
        }
        g
    }
}